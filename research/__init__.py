"""Literature research (spec: the factory reads papers — arXiv/HF — on agents that
drive a CLI, and distills techniques into grounded, *staged* proposals). Retrieval
is deterministic Python (no LLM tools); the LLM only distills the fetched text, so
the researcher role stays isolated like every other `claude -p` role."""
