# clive-harness-factory dependencies (Phase 0).
# The factory tooling runs under system python3 and leans on the stdlib
# (sqlite3, http.server). The only third-party need is YAML.
#
#   pip install -r factory/requirements.txt --break-system-packages
#
# clive itself supplies the runtime the candidate is driven with (anthropic /
# openai / libtmux / python-dotenv); see ../requirements.txt. The `claude` CLI
# (Claude Code) must be on PATH for the claude -p roles.
pyyaml>=6.0
